npm i typescript

npx tsc --init (cria arquivo de configuração)

ir no arquivo tsconfig e descomentar a configurar o atributo outdir (especificar a pasta que o arquivo JavaScript vai receber o codigo ts transpilado)

npx tsc

npx tsc --watch (sempre que salvar o código ele vai transpilar TS o código pra um arquivo JS)
